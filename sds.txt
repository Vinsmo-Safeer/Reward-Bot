//    public static void main(String[] args) {
//        loadSettings();
//
//        System.out.println("Max searches per day: " + settings.getMaxSearchPerDay());
//        System.out.println("Chrome path: " + settings.getChromePath());
//        System.out.println("User data directory: " + settings.getUserDataDir());
//        System.out.println("Enable Proxy: " + settings.isEnableProxy());
//
//        if (settings.isEnableProxy() && settings.getProxySettings() != null) {
//            ProxyConfig proxy = settings.getProxySettings();
//
//            if (proxy.isUseStaticProvider()) {
//                // Double check they didn't leave the list empty
//                if (proxy.getStaticProxies() == null || proxy.getStaticProxies().isEmpty()) {
//                    System.err.println("Error: Static provider enabled, but no proxies typed!");
//                } else {
//                    System.out.println("Loading purchased proxies...");
//                    for (String ip : proxy.getStaticProxies()) {
//                        System.out.println("Loaded Proxy: " + ip);
//                    }
//                }
//            }
//
//            else if (proxy.isUseWebshareApi()) {
//                if (proxy.getWebshareKeys() == null || proxy.getWebshareKeys().isEmpty()) {
//                    System.err.println("Error: Webshare API enabled, but no keys typed!");
//                } else {
//                    System.out.println("Fetching proxies via Webshare APIs...");
//                    for (String apiKey : proxy.getWebshareKeys()) {
//                        System.out.println("Using API Key: " + apiKey);
//                    }
//                }
//            }
//        } else {
//            System.out.println("Running without any proxies.");
//        }
//    }